package io.github.gipo999.smispi;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/** SPI (Service Provider Interface) for the core module. */
public final class SmiSpi {

  /** Logger for the SPI. */
  static final Logger LOGGER = LoggerFactory.getLogger(SmiSpi.class);

  /** private constructor to prevent instantiation. */
  private SmiSpi() {}
}
